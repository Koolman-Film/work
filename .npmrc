engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# We use pnpm-workspace.yaml only for settings (single-package repo).
# Suppress "adding to root" warning on pnpm add.
ignore-workspace-root-check=true
